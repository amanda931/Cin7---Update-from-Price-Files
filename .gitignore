# Logs and runtime output
Logs/
*.log

# Python cache
__pycache__/
*.pyc

# Input data — supplier price file changes constantly
*.csv

# Export-mode scan cache (rebuilds itself; can get large)
export_cache.json

# Safety net — never commit credentials, even a stray local copy
Credentials.txt